import sqlite3 from 'better-sqlite3';
const db = sqlite3('./global.db');

db.prepare(`
  CREATE TABLE IF NOT EXISTS settings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username TEXT UNIQUE NOT NULL,
    data TEXT NOT NULL
  )
`).run();

export default db;
